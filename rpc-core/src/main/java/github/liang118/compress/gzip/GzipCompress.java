package github.liang118.compress.gzip;

import github.liang118.compress.Compress;

public class GzipCompress implements Compress {

    @Override
    public byte[] compress(byte[] bytes) {
        return new byte[0];
    }
}
